<UserControl x:Class="WPF.PresentationLayer.Views.Import.GoodsReceiptListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F5F7FA"
             Height="521"
             Width="818">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Danh sách phiếu nhập"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#1F2937"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,16">
            <Button Content="Tải lại"
                    Width="100"
                    Height="36"
                    Click="BtnRefresh_Click"/>
            <Button Content="Thêm phiếu nhập"
                    Width="140"
                    Height="36"
                    Margin="12,0,0,0"
                    Click="BtnAdd_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgGoodsReceipts"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã phiếu" Binding="{Binding GrnNumber}" Width="150"/>
                <DataGridTextColumn Header="Ngày nhập" Binding="{Binding ReceiptDate}" Width="150"/>
                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount}" Width="150"/>
                <DataGridTextColumn Header="Ghi chú" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>