<UserControl x:Class="WPF.PresentationLayer.Views.Admin.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WPF.PresentationLayer.ViewModels.Admin">

    <UserControl.DataContext>
        <vm:UserManagementViewModel/>
    </UserControl.DataContext>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Tạo tài khoản mới"
                    Style="{StaticResource ButtonPrimary}"
                    Width="160"
                    Command="{Binding CreateCommand}"/>

            <TextBox Grid.Column="2"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Tag="Tìm kiếm người dùng...">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="Tìm kiếm người dùng..."
                                                           Foreground="#AAAAAA"
                                                           FontSize="12"
                                                           Margin="10,0"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Users}"
                  SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeRows="False"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#EEEEEE"
                  BorderBrush="{StaticResource BrushBorder}"
                  BorderThickness="1"
                  Background="{StaticResource BrushSurface}"
                  RowBackground="{StaticResource BrushSurface}"
                  AlternatingRowBackground="#FAFAFA"
                  SelectionMode="Single"
                  HeadersVisibility="Column">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background"       Value="#F5F5F5"/>
                    <Setter Property="Foreground"       Value="{StaticResource BrushTextSecondary}"/>
                    <Setter Property="FontSize"         Value="11"/>
                    <Setter Property="FontWeight"       Value="SemiBold"/>
                    <Setter Property="Padding"          Value="12,10"/>
                    <Setter Property="BorderBrush"      Value="#E0E0E0"/>
                    <Setter Property="BorderThickness"  Value="0,0,0,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding"         Value="12,8"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FontSize"        Value="13"/>
                    <Setter Property="Foreground"      Value="{StaticResource BrushTextPrimary}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#EEF0F2"/>
                            <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Họ và tên"
                                    Binding="{Binding FullName}"
                                    Width="200"/>
                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="220"/>
                <DataGridTextColumn Header="Mã NV"
                                    Binding="{Binding StaffCode}"
                                    Width="100"/>
                <DataGridTextColumn Header="Vai trò"
                                    Binding="{Binding Role.RoleName}"
                                    Width="130"/>
                <DataGridTextColumn Header="Ngày tạo"
                                    Binding="{Binding CreatedAt, StringFormat=dd/MM/yyyy}"
                                    Width="110"/>
                <DataGridTemplateColumn Header="Trạng thái" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock VerticalAlignment="Center" Margin="12,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Text"       Value="Hoạt động"/>
                                                <Setter Property="Foreground" Value="#2E7D32"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Text"       Value="Vô hiệu"/>
                                                <Setter Property="Foreground" Value="#B00020"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Bar -->
        <Border Grid.Row="3"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="1"
                Padding="12,10"
                Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{StaticResource BrushTextSecondary}">
                    <TextBlock.Text>
                        <Binding Path="Selected.Email"
                                 StringFormat="Đã chọn: {0}"
                                 FallbackValue="Chưa chọn người dùng nào"/>
                    </TextBlock.Text>
                </TextBlock>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right"
                            Visibility="{Binding ActionButtonsVisible}">
                    <Button Content="Xóa tài khoản"
                            Width="130"
                            Margin="0,0,8,0"
                            Command="{Binding DeleteCommand}"
                            Background="#B00020"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="12,6"
                            Cursor="Hand"/>
                    <Button Content="{Binding ToggleActiveLabel}"
                            Style="{StaticResource ButtonPrimary}"
                            Width="140"
                            Command="{Binding ToggleActiveCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
