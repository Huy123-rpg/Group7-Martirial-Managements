<UserControl x:Class="WPF.PresentationLayer.Views.Export.SalesOrderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:WPF.PresentationLayer.Helpers.Converters"
             Background="#F5F7FA">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Đơn bán hàng (Sales Order)"
                   FontSize="24" FontWeight="Bold" Foreground="#1F2937"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,16">
            <Button Content="Tải lại" Width="100" Height="36" Click="BtnRefresh_Click"/>
            <Button Content="Thêm SO" Width="120" Height="36" Margin="12,0,0,0" Click="BtnAdd_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgSalesOrders" Grid.Row="2"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Số SO" Binding="{Binding SoNumber}" Width="130"/>
                <DataGridTextColumn Header="Khách hàng" Binding="{Binding CustomerName}" Width="160"/>
                <DataGridTextColumn Header="Kho" Binding="{Binding WarehouseName}" Width="120"/>
                <DataGridTextColumn Header="Ngày đặt" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalAmount, StringFormat=N0}" Width="110"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding StatusText}" Width="90"/>
                <DataGridTemplateColumn Header="Hành động" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <Button Content="Duyệt" Width="60" Margin="0,0,4,0"
                                        Background="#10B981" Foreground="White"
                                        Visibility="{Binding IsApproveVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Click="BtnApprove_Click"/>
                                <Button Content="Tạo phiếu xuất" Width="105" Margin="0,0,4,0"
                                        Background="#3B82F6" Foreground="White"
                                        Visibility="{Binding IsCreateIssueVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Click="BtnCreateIssue_Click"/>
                                <Button Content="Sửa" Width="50" Margin="0,0,4,0"
                                        Visibility="{Binding IsEditVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Click="BtnEdit_Click"/>
                                <Button Content="Xóa" Width="50"
                                        Background="#EF4444" Foreground="White"
                                        Visibility="{Binding IsDeleteVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Click="BtnDelete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
